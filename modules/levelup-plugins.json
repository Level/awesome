{
  "title": "Plugins for levelup",
  "description": [
    "Modules that add functionality to [levelup]."
  ],
  "modules": {
    "levelup-async-iterator": {
      "github": "MeirionHughes/levelup-async-iterator",
      "description": "Expose `iterator()` with [`Symbol.asyncIterator`](https://github.com/tc39/proposal-async-iteration)."
    },
    "cluster-levelup": {
      "github": "chiguireitor/cluster-levelup",
      "description": "Wrap any [`levelup`][levelup] instance for [`cluster`](https://nodejs.org/api/cluster.html) usage among multiple processes."
    },
    "level-ttl": {
      "github": "Level/level-ttl",
      "description": [
        "Add a `ttl` (time-to-live) option to [levelup] for `put()` and `batch()`.",
        "Note: suffers from race issues."
      ],
      "copyrightYear": 2013,
      "author": "Rod Vagg"
    }
  }
}
